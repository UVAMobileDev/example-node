var nameNew = 'Josh';
console.log(nameNew);

//Primative variable
//stored directly in the location that the variable accesses
//stack
//string, #s, booleans, nulls (intentionally empty), undefined (default), symbols


//Reference type
//object stored on the heap
//pointer in memory to the variable's location
//arrays, object literals, functions, dates, anything else

//int number = 4;
var myNum = 4;
myNum = 'String';
